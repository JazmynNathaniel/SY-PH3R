# Local development defaults
PORT=4300
HOST=127.0.0.1
NODE_ENV=development
SESSION_SECRET=replace-with-64-char-random-hex-secret
DB_PATH=./data/sy-ph3r.db

# Hosted deployment overrides
# Render:
# HOST=0.0.0.0
# NODE_ENV=production
# DB_PATH=./apps/relay/data/sy-ph3r.db
